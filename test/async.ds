using system::async
using system::io
using system

module helloWorld

async fn doAsyncStuff(int i)
{
	if i == 0
	{
		return
	}

	await sleep()
	writeInt(i)

	await doAsyncStuff(i - 1)
}

[EntryPoint]
fn main()
{
	_ = doAsyncStuff(5)
}
