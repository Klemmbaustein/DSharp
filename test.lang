using system

fn useLambda(fn() callback)
{
	callback.call()
}

[EntryPoint]
fn main() -> int
{
	string msg = "hi"
	string msg2 = "world"

	useLambda(fn() {
		io::writeln(msg)
		io::writeln(msg2)
	})

	return 0
}
