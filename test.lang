using system

module test

fn yellString(string in) -> string
{
	while in.length < 50
	{
		in = in + "!"
	}
	return in
}

[EntryPoint]
fn main() -> int
{
	string initial = "hello"
	println(yellString(initial))

	return 1
}