using system::io
using system::fs

module commands

class Command
{
	string name

	virtual fn execute()
	{

	}
}

class ListCommand : Command
{
	fn new()
	{
		this.name = "ls"
	}

	override fn execute()
	{
		const path = new FilePath(".")
		FilePath[] paths = path.getFiles()

		int i = 0

		while i < paths.length
		{
			writeln(paths[i].pathString)
			i += 1
		}
	}
}

class CurrentDirCommand : Command
{
	fn new()
	{
		this.name = "pwd"
	}

	override fn execute()
	{
		writeln(getCurrentPath().full())
	}
}
